% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{load_btr1}
\alias{load_btr1}
\title{Load a complete BTR1 extraction into the database}
\usage{
load_btr1(result, con, mode = "append")
}
\arguments{
\item{result}{The list returned by \code{\link[=extract_btr1_uf]{extract_btr1_uf()}} or
\code{\link[=extract_btr1_all]{extract_btr1_all()}}, containing \verb{$data} and \verb{$metadata}.}

\item{con}{A \code{DBIConnection} object.}

\item{mode}{Character. Write mode for factEmissions.
\code{"append"} (default) or \code{"overwrite"}.}
}
\value{
Invisible \code{TRUE} on success.
}
\description{
Takes the output of \code{\link[=extract_btr1_uf]{extract_btr1_uf()}} or \code{\link[=extract_btr1_all]{extract_btr1_all()}}
and loads both the metadata (dimYear) and fact data (factEmissions)
in one step. Ensures the schema exists before writing.
}
\examples{
\dontrun{
  con <- db_connect()
  db_create_schema(con)

  result <- extract_btr1_uf("BTR1_UF_Energia.xlsx")
  load_btr1(result, con)

  db_disconnect(con)
}
}
