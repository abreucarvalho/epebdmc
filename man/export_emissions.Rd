% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_emissions}
\alias{export_emissions}
\title{Export emissions data to a file}
\usage{
export_emissions(
  path,
  sector = NULL,
  uf = NULL,
  gas = "CO2 eq",
  year = NULL,
  region = NULL,
  includeBrasil = FALSE,
  dbname = "epebdmc.sqlite",
  ...
)
}
\arguments{
\item{path}{Character. Output file path. The extension determines
the format: \code{.csv} for CSV, \code{.xlsx} for Excel.}

\item{sector}{Character vector. Sector names in English or Portuguese.
E.g. \code{"Energy"}, \code{"Energia"}, \code{"LULUCF"}. Default: all sectors.}

\item{uf}{Character vector. State codes (\code{"SP"}, \code{"RJ"}),
full names (\code{"São Paulo"}), or IBGE codes (35). Default: all states.}

\item{gas}{Character vector. Gas names: \code{"CO2"}, \code{"CH4"}, \code{"N2O"},
\code{"CO2 eq"}, \code{"HFCs"}, \code{"PFCs"}, \code{"SF6"}. Default: \code{"CO2 eq"}.}

\item{year}{Integer vector or range. E.g. \code{2022}, \code{2010:2022},
\code{c(1990, 2000, 2010, 2022)}. Default: all years.}

\item{region}{Character vector. Region names: \code{"Norte"}, \code{"Nordeste"},
\code{"Sudeste"}, \code{"Sul"}, \code{"Centro-Oeste"}. Default: all regions.}

\item{includeBrasil}{Logical. Include the Brasil total row?
Default \code{FALSE}.}

\item{dbname}{Character. Path to the database. Default \code{"epebdmc.sqlite"}.}

\item{...}{Additional filter arguments passed to \code{\link[=get_emissions]{get_emissions()}}.}
}
\value{
Invisible tibble of the exported data.
}
\description{
Queries the database with the same filters as \code{\link[=get_emissions]{get_emissions()}}
and saves the result to CSV or Excel format.
}
\examples{
\dontrun{
  # Export Energy sector as CSV
  export_emissions(
    path   = "energy_emissions.csv",
    sector = "Energy"
  )

  # Export all data as Excel
  export_emissions(path = "all_emissions.xlsx")

  # Filtered export
  export_emissions(
    path   = "sudeste_2022.csv",
    region = "Sudeste",
    year   = 2022
  )
}
}
