% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_ranking}
\alias{plot_ranking}
\title{Plot emissions as a bar chart for a single year}
\usage{
plot_ranking(
  sector = NULL,
  uf = NULL,
  gas = "CO2 eq",
  year = NULL,
  region = NULL,
  includeBrasil = FALSE,
  rankBy = "uf",
  topN = NULL,
  title = NULL,
  dbname = "epebdmc.sqlite"
)
}
\arguments{
\item{sector}{Character vector. Sector names in English or Portuguese.
E.g. \code{"Energy"}, \code{"Energia"}, \code{"LULUCF"}. Default: all sectors.}

\item{uf}{Character vector. State codes (\code{"SP"}, \code{"RJ"}),
full names (\code{"São Paulo"}), or IBGE codes (35). Default: all states.}

\item{gas}{Character vector. Gas names: \code{"CO2"}, \code{"CH4"}, \code{"N2O"},
\code{"CO2 eq"}, \code{"HFCs"}, \code{"PFCs"}, \code{"SF6"}. Default: \code{"CO2 eq"}.}

\item{year}{Integer. A single year. Default: most recent year.}

\item{region}{Character vector. Region names: \code{"Norte"}, \code{"Nordeste"},
\code{"Sudeste"}, \code{"Sul"}, \code{"Centro-Oeste"}. Default: all regions.}

\item{includeBrasil}{Logical. Include the Brasil total row?
Default \code{FALSE}.}

\item{rankBy}{Character. How to group bars:
\code{"uf"} (default), \code{"sector"}, or \code{"region"}.}

\item{topN}{Integer. Show only the top N entries. Default: all.}

\item{title}{Character. Plot title. Auto-generated if \code{NULL}.}

\item{dbname}{Character. Path to the database. Default \code{"epebdmc.sqlite"}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a horizontal bar chart comparing states, sectors, or
regions for a given year.
}
\examples{
\dontrun{
  # Top 10 emitting states in 2022, Energy sector
  plot_ranking(sector = "Energy", year = 2022, topN = 10)

  # Compare sectors in 2022
  plot_ranking(year = 2022, rankBy = "sector")

  # Compare regions for LULUCF
  plot_ranking(sector = "LULUCF", year = 2022, rankBy = "region")
}
}
