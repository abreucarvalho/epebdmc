% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_emissions}
\alias{plot_emissions}
\title{Plot emissions over time}
\usage{
plot_emissions(
  sector = NULL,
  uf = NULL,
  gas = "CO2 eq",
  year = NULL,
  region = NULL,
  includeBrasil = FALSE,
  colorBy = "uf",
  title = NULL,
  showTotal = FALSE,
  dbname = "epebdmc.sqlite"
)
}
\arguments{
\item{sector}{Character vector. Sector names in English or Portuguese.
E.g. \code{"Energy"}, \code{"Energia"}, \code{"LULUCF"}. Default: all sectors.}

\item{uf}{Character vector. State codes (\code{"SP"}, \code{"RJ"}),
full names (\code{"São Paulo"}), or IBGE codes (35). Default: all states.}

\item{gas}{Character vector. Gas names: \code{"CO2"}, \code{"CH4"}, \code{"N2O"},
\code{"CO2 eq"}, \code{"HFCs"}, \code{"PFCs"}, \code{"SF6"}. Default: \code{"CO2 eq"}.}

\item{year}{Integer vector or range. E.g. \code{2022}, \code{2010:2022},
\code{c(1990, 2000, 2010, 2022)}. Default: all years.}

\item{region}{Character vector. Region names: \code{"Norte"}, \code{"Nordeste"},
\code{"Sudeste"}, \code{"Sul"}, \code{"Centro-Oeste"}. Default: all regions.}

\item{includeBrasil}{Logical. Include the Brasil total row?
Default \code{FALSE}.}

\item{colorBy}{Character. What to map to color/lines:
\code{"uf"} (default), \code{"sector"}, \code{"gas"}, or \code{"region"}.}

\item{title}{Character. Plot title. Auto-generated if \code{NULL}.}

\item{showTotal}{Logical. Show a dashed total line? Default \code{FALSE}.}

\item{dbname}{Character. Path to the database. Default \code{"epebdmc.sqlite"}.}
}
\value{
A ggplot2 object (can be further customized with \code{+}).
}
\description{
Creates a line chart of emissions by year. All filters from
\code{\link[=get_emissions]{get_emissions()}} are available. The \code{colorBy} parameter controls
how lines are grouped and colored.
}
\examples{
\dontrun{
  # Emissions by state in the Energy sector
  plot_emissions(sector = "Energy", region = "Sudeste")

  # Compare sectors for S\u00e3o Paulo
  plot_emissions(uf = "SP", colorBy = "sector")

  # Compare gases for Brasil
  plot_emissions(
    includeBrasil = TRUE, uf = "BR",
    gas = c("CO2", "CH4", "N2O"),
    colorBy = "gas"
  )

  # By region
  plot_emissions(sector = "LULUCF", colorBy = "region")
}
}
