% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{plot_map}
\alias{plot_map}
\title{Plot emissions on a Brazil map}
\usage{
plot_map(
  sector = "Total",
  gas = "CO2 eq",
  year = NULL,
  region = NULL,
  title = NULL,
  palette = "YlOrRd",
  showValues = FALSE,
  dbname = "epebdmc.sqlite"
)
}
\arguments{
\item{sector}{Character. Sector name. Default: \code{"Total"}.}

\item{gas}{Character. Gas name. Default: \code{"CO2 eq"}.}

\item{year}{Integer. A single year to display. Default: most recent year.}

\item{region}{Character vector. Region names: \code{"Norte"}, \code{"Nordeste"},
\code{"Sudeste"}, \code{"Sul"}, \code{"Centro-Oeste"}. Default: all regions.}

\item{title}{Character. Plot title. Auto-generated if \code{NULL}.}

\item{palette}{Character. Color palette name from
\code{\link[ggplot2:scale_brewer]{ggplot2::scale_fill_distiller()}}. Default: \code{"YlOrRd"}.}

\item{showValues}{Logical. Show emission values as labels on each
state? Default \code{FALSE}.}

\item{dbname}{Character. Path to the database.}
}
\value{
A ggplot2 object.
}
\description{
Creates a choropleth map showing emissions by state for a given
year. States are colored by emission intensity. All filters from
\code{\link[=get_emissions]{get_emissions()}} are available.
}
\examples{
\dontrun{
  # Total CO2 eq emissions by state in 2022
  plot_map()

  # Energy sector
  plot_map(sector = "Energy", year = 2022)

  # LULUCF with values displayed
  plot_map(sector = "LULUCF", year = 2022, showValues = TRUE)

  # CH4 emissions from Agriculture
  plot_map(sector = "Agriculture", gas = "CH4", year = 2015)

  # Custom color palette
  plot_map(sector = "Energy", palette = "Blues")
}
}
